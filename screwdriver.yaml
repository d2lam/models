shared:
    image: node:6

jobs:
    main:
        steps:
            - metaset: meta set foo barrrrr
        requires: [~commit, ~pr]
    nextJob:
        steps:
            - echo: echo bye
        requires: main
