shared:
    image: node:6

jobs:
    main:
        annotations:
            beta.screwdriver.cd/executor: queue
        steps:
            - metaset: meta set foo_bar 11111
            - metaget: meta set foo_bar
        requires: [~commit, ~pr]
    newPRJob:
        steps:
            - echo: echo bye
        requires: [~pr]
